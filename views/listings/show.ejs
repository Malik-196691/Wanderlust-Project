<% layout('layouts/broilerplate') %>

<body class="bg-light">
  <!-- Hero Banner -->
  <header
    class="position-relative w-100"
    style="
      height: 80vh;
      background: url('<%= listing.image.url %>') center center / cover
        no-repeat fixed;
    "
  >
    <div
      class="position-absolute bottom-0 start-0 w-100 p-5"
      style="background: linear-gradient(transparent, rgba(0, 0, 0, 0.8))"
    >
      <h1 class="text-white fw-bold display-3 mb-2"><%= listing.title %></h1>
      <h5 class="text-light">
        <i class="bi bi-geo-alt-fill me-2"></i>
        <%= listing.location %>, <%= listing.country %>
      </h5>
    </div>
  </header>

  <!-- Main Content -->
  <main class="container py-5">
    <div class="row justify-content-center">
      <div class="col-lg-10">
        <!-- Description -->
        <section class="mb-5">
          <h2 class="text-primary fw-bold mb-4 border-bottom pb-2">
            <i class="bi bi-info-circle me-2"></i> Listing Details
          </h2>
          <p class="text-secondary fs-5"><%= listing.description %></p>
        </section>

        <!-- Information Boxes -->
        <section class="row g-4 mb-5">
          <div class="col-md-4">
            <div class="bg-white shadow-sm p-4 rounded-4 text-center h-100">
              <i class="bi bi-cash-stack text-success fs-2 mb-2"></i>
              <h6 class="text-muted">Price</h6>
              <h4 class="fw-bold text-dark">$<%= listing.price %></h4>
            </div>
          </div>
          <div class="col-md-4">
            <div class="bg-white shadow-sm p-4 rounded-4 text-center h-100">
              <i class="bi bi-geo-alt text-primary fs-2 mb-2"></i>
              <h6 class="text-muted">Location</h6>
              <h4 class="fw-bold text-dark"><%= listing.location %></h4>
            </div>
          </div>
          <div class="col-md-4">
            <div class="bg-white shadow-sm p-4 rounded-4 text-center h-100">
              <i class="bi bi-globe text-info fs-2 mb-2"></i>
              <h6 class="text-muted">Country</h6>
              <h4 class="fw-bold text-dark"><%= listing.country %></h4>
            </div>
          </div>
        </section>

        <!-- Edit/Delete Buttons -->
        <% if (currentUser && listing.owner && String(currentUser._id) ===
        String(listing.owner._id || listing.owner)) { %>
        <div class="d-flex justify-content-end gap-3">
          <a
            href="/listings/<%= listing._id %>/edit"
            class="btn btn-lg btn-primary px-5 fw-semibold shadow-sm"
          >
            <i class="bi bi-pencil-square me-2"></i>Edit
          </a>
          <form
            action="/listings/<%= listing._id %>?_method=DELETE"
            method="POST"
            style="display: inline"
          >
            <button
              type="submit"
              class="btn btn-lg btn-danger px-5 fw-semibold shadow-sm"
            >
              <i class="bi bi-trash me-2"></i>Delete
            </button>
          </form>
        </div>
        <% } %>

        <!-- Reviews Section -->

        <div class="mt-5">
          <h4>Leave a Review</h4>
          <form
            action="/listings/<%= listing._id %>/reviews"
            method="POST"
            class="needs-validation review-form"
            novalidate
          >
            <!-- ✅ Dynamic Starability Rating -->
            <fieldset class="starability-slot">
              <input
                type="radio"
                id="no-rate"
                class="input-no-rate"
                name="review[rating]"
                value="0"
                checked
                aria-label="No rating."
              />
              <input
                type="radio"
                id="first-rate1"
                name="review[rating]"
                value="1"
              />
              <label for="first-rate1" title="Terrible">1 star</label>

              <input
                type="radio"
                id="first-rate2"
                name="review[rating]"
                value="2"
              />
              <label for="first-rate2" title="Not good">2 stars</label>

              <input
                type="radio"
                id="first-rate3"
                name="review[rating]"
                value="3"
              />
              <label for="first-rate3" title="Average">3 stars</label>

              <input
                type="radio"
                id="first-rate4"
                name="review[rating]"
                value="4"
              />
              <label for="first-rate4" title="Very good">4 stars</label>

              <input
                type="radio"
                id="first-rate5"
                name="review[rating]"
                value="5"
              />
              <label for="first-rate5" title="Amazing">5 stars</label>
            </fieldset>

            <div class="mb-3">
              <label for="reviewText" class="form-label">Your Review</label>
              <textarea
                class="form-control"
                id="reviewText"
                name="review[text]"
                rows="3"
                required
              ></textarea>
            </div>

            <button type="submit" class="btn btn-primary mb-4">
              Submit Review
            </button>
          </form>
        </div>

        <br />
        <% if (listing.reviews.length != 0) { %>
        <h4>All Reviews</h4>
        <% } %>
        <ul class="review-list list-group">
          <% for (let review of listing.reviews) { %>
          <li class="list-group-item mb-2">
            <div>
              <p
                class="starability-result"
                data-rating="<%= review.rating %>"
              ></p>
              <strong>By:</strong> <%= review.author.username %><br />
              <strong>Comment:</strong> <%= review.text %>
            </div>

            <% if (currentUser && review.author && String(currentUser._id) ===
            String(review.author._id || review.author)) { %>
            <form
              action="/listings/<%= listing._id %>/reviews/<%= review._id %>?_method=DELETE"
              method="POST"
              style="display: inline"
            >
              <button class="btn btn-danger btn-sm mt-2">
                <i class="fa-solid fa-trash"></i>
              </button>
            </form>
            <% } %>
          </li>
          <% } %>
        </ul>

        <!-- 🗺️ MAP SECTION -->
        <h3 class="mt-5 mb-3 fw-bold">Map Location</h3>
        <div id="map" style="height: 400px; border-radius: 10px"></div>
      </div>
    </div>
  </main>

  <!-- Leaflet JS -->
  <script src="https://unpkg.com/leaflet@1.9.3/dist/leaflet.js"></script>
  <script>
    window.listingData = {
      title: "<%= listing.title %>",
      location: "<%= listing.location %>",
      country: "<%= listing.country %>",
    };
  </script>
  <script src="/js/map.js"></script>
</body>
